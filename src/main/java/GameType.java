public enum GameType {
    /**
     * Classic mode: When logged in, full stakes. Money can be lost at any missed question
     * Infinite mode: It's so infinite it's impossible to exit :)))
     */
    CLASSIC,
    INFINITE
}
